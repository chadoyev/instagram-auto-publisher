# 📖 Руководство по миграции на v2.0

Это руководство поможет вам перейти со старой версии (main.py + loopbot.py) на новую модульную версию 2.0.

---

## 🆕 Что изменилось?

### Структура проекта

**Было:**
```
instagram-auto-publisher/
├── main.py           (1354 строки - всё в одном файле)
├── loopbot.py        (368 строк - планировщик)
├── get_json.py       (вспомогательный скрипт)
└── db.db             (база данных)
```

**Стало:**
```
instagram-auto-publisher/
├── src/              (модули)
│   ├── config.py     (конфигурация)
│   ├── database.py   (работа с БД)
│   ├── downloaders.py (загрузка контента)
│   ├── uploaders.py  (публикация)
│   ├── scheduler.py  (планировщик)
│   ├── bot.py        (Telegram бот)
│   └── utils.py      (утилиты)
├── app.py            (точка входа)
├── Dockerfile        (Docker образ)
├── docker-compose.yml
└── .env              (секреты и конфигурация)
```

### Ключевые изменения

1. **Конфигурация**: Все токены теперь в `.env` файле
2. **Модульность**: Код разделён на логические модули
3. **Docker**: Готовая поддержка контейнеризации
4. **Безопасность**: SQL-инъекции защищены, thread-safe БД
5. **Планировщик**: Переработан с нуля, более стабильный

---

## 🔄 Пошаговая миграция

### Шаг 1: Бэкап данных

```bash
# Создайте резервные копии
cp db.db db_backup.db
cp authorize.json authorize_backup.json

# Опционально: сохраните весь контент
tar -czf content_backup.tar.gz storys/ video_posts/ photo_posts/ albums_posts/ users_content/
```

### Шаг 2: Остановите старую версию

Если у вас запущен старый бот:
- Нажмите `Ctrl+C` для остановки
- Убедитесь что процесс завершён

### Шаг 3: Обновите код

```bash
# Получите последние изменения
git pull origin main

# Или клонируйте заново
git clone https://github.com/chadoyev/instagram-auto-publisher.git
cd instagram-auto-publisher
```

### Шаг 4: Создайте .env файл

```bash
# Скопируйте пример
cp env.example.txt .env
```

Заполните `.env` вашими данными из старого `main.py`:

| Старое место | Новое место в .env |
|-------------|-------------------|
| `token = "..."` (строка 24) | `TELEGRAM_BOT_TOKEN=...` |
| `tg1 = ...` (строка 26) | `TELEGRAM_ADMIN_ID_1=...` |
| `tg2 = ...` (строка 27) | `TELEGRAM_ADMIN_ID_2=...` |
| `ip_host = '...'` (строка 23) | `TIKTOK_API_HOST=...` |
| `API_KEY` в коде | `HIKER_API_KEY=...` |
| `name_pablik = "..."` (строка 33) | `DEFAULT_CAPTION=...` |

**Пример .env:**
```env
INSTAGRAM_USERNAME=myaccount
INSTAGRAM_PASSWORD=mypassword123
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_ADMIN_ID_1=987654321
TELEGRAM_ADMIN_ID_2=123456789
HIKER_API_KEY=your_api_key_here
TIKTOK_API_HOST=api.example.com
DEFAULT_CAPTION=Подписывайся на наш канал!
```

### Шаг 5: Перенесите данные

Ваши данные уже в правильных местах! Просто убедитесь что:

```bash
# Эти файлы/папки есть и содержат данные
ls -la db.db              # База данных
ls -la authorize.json     # Instagram сессия
ls -la storys/            # Stories
ls -la video_posts/       # Видео посты
ls -la photo_posts/       # Фото посты
ls -la albums_posts/      # Альбомы
```

### Шаг 6: Установите зависимости

```bash
# Локально
pip install -r requirements.txt

# Или используйте Docker (рекомендуется)
# - в этом случае зависимости установятся автоматически
```

### Шаг 7: Запустите новую версию

**Вариант A: Docker (рекомендуется)**
```bash
docker-compose up -d
docker-compose logs -f
```

**Вариант B: Локально**
```bash
python app.py
```

---

## ✅ Проверка работоспособности

После запуска проверьте:

1. **Бот отвечает в Telegram**
   - Отправьте `/start`
   - Должно прийти приветственное сообщение

2. **Админ-панель работает**
   - Отправьте `sliska`
   - Должно открыться меню

3. **Статистика загружается**
   - В админ-панели выберите "📊 Статистика"
   - Должны увидеть количество файлов

4. **Автопостинг настраивается**
   - В админ-панели выберите "⚙️ Автопостинг"
   - Проверьте что настройки из БД загрузились

---

## 🔍 Сравнение кода

### Было (main.py):
```python
# Хардкоженные токены
token = "123456789:ABC..."
tg1 = 123456
ip_host = 'api.example.com'

# Небезопасные SQL запросы
cursor.execute(f"SELECT * FROM users WHERE user_id = {user_id}")
```

### Стало:
```python
# Токены в .env
from src.config import Config
token = Config.TELEGRAM_BOT_TOKEN
admin_ids = Config.get_admin_ids()

# Безопасные параметризованные запросы
db.cursor.execute("SELECT * FROM users WHERE user_id = ?", (user_id,))
```

---

## 🐛 Решение проблем

### "Не могу найти модуль src"

**Решение:**
```bash
# Убедитесь что вы в корне проекта
cd instagram-auto-publisher

# Запускайте через app.py (он добавляет src в PATH)
python app.py
```

### "Ошибка конфигурации"

**Решение:**
- Проверьте что `.env` файл существует
- Убедитесь что все обязательные поля заполнены
- Сравните с `env.example.txt`

### "База данных не найдена"

**Решение:**
```bash
# Убедитесь что db.db в корне проекта
ls -la db.db

# Если нет, скопируйте бэкап
cp db_backup.db db.db
```

### "Instagram не авторизуется"

**Решение:**
```bash
# Пересоздайте сессию
python setup_instagram.py
```

---

## 📊 Совместимость данных

Хорошая новость: **Ваша база данных полностью совместима!**

Новая версия:
- ✅ Читает старую БД без изменений
- ✅ Добавляет новые таблицы автоматически
- ✅ Сохраняет все ваши данные
- ✅ Работает со старыми путями к файлам

---

## 🎓 Изучение нового кода

Начните с этих файлов:

1. **app.py** - точка входа, понятная структура запуска
2. **src/config.py** - посмотрите как работают ENV переменные
3. **src/bot.py** - знакомая логика Telegram бота, но чище
4. **src/scheduler.py** - новый планировщик (vs старый loopbot.py)

---

## 💡 Советы

1. **Не удаляйте старые файлы** (main.py.backup, loopbot.py.backup) пока не убедитесь что всё работает
2. **Делайте бэкапы** перед экспериментами
3. **Используйте Docker** - проще в управлении
4. **Читайте логи** - они теперь более информативны

---

## 📞 Нужна помощь?

- 📖 Документация: [README.md](README.md)
- 🚀 Быстрый старт: [QUICKSTART.md](QUICKSTART.md)
- 💬 Telegram: [@chadoyev](https://t.me/chadoyev)
- 🐛 Баги: [GitHub Issues](https://github.com/chadoyev/instagram-auto-publisher/issues)

---

**Удачной миграции! 🚀**

